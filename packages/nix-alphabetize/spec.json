{
  "testCommand": "nix fmt && nix build .#checks.x86_64-linux.nix-alphabetize",
  "selfEditingRules": [
    "Keep this file intent-focused and describe user-visible behavior and constraints rather than implementation details.",
    "Update features and scenarios whenever the implemented behavior changes and the new behavior is intended.",
    "Add, merge, split, or remove features and scenarios to keep the spec aligned with the current intended product behavior.",
    "Prefer adjusting existing scenarios over adding redundant ones when intent can be expressed more clearly by rewriting.",
    "If code and tests conflict with this file, first determine whether the implementation is intentionally different; if so, update this file to match the intended behavior.",
    "Do not change testCommand unless explicitly instructed."
  ],
  "features": [
    {
      "feature": "Nix source formatting",
      "scenarios": [
        {
          "scenario": "User formats Nix files",
          "given": ["the user provides one or more Nix file paths"],
          "when": ["the formatter rewrites the files"],
          "then": [
            "the formatter sorts attribute bindings into a stable order",
            "the formatter can collapse or expand nested attribute paths into a normalized representation",
            "the formatter rewrites the source in place"
          ]
        },
        {
          "scenario": "User formats syntax that should preserve ordering intent",
          "given": [
            "the input contains constructs where original ordering is meaningful"
          ],
          "when": ["the formatter rewrites the file"],
          "then": [
            "the formatter preserves string-list ordering",
            "the formatter preserves multiline string content",
            "the formatter keeps the output parseable as Nix"
          ]
        }
      ]
    }
  ]
}
