[dependencies]
ply-engine = "0.1.0"
serde = {version = "1.0", features = ["derive"]}
serde_json = "1.0"
reqwest = {version = "0.12", features = ["json"]}
tokio = {version = "1", features = ["full"]}

[dev-dependencies]

[package]
name = "ply-engine-postgres"
version = "0.0.0"
edition = "2021"
