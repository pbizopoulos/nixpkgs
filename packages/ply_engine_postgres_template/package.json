{
  "name": "ply_engine_postgres_template",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "cargo run",
    "format": "nix fmt",
    "build": "cargo build --release",
    "start": "cargo run --release",
    "test": "npm run test:unit && npm run test:cpd",
    "test:unit": "cargo test",
    "test:cpd": "jscpd .",
    "db:start": "postgres start",
    "db:stop": "postgres stop"
  },
  "devDependencies": {
    "jscpd": "^4.0.5"
  }
}
