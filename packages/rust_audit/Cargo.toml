[dependencies]
anyhow = "1.0"
clap = {version = "4.5", features = ["derive"]}
tokio = {version = "1.0", features = ["full"]}

[package]
name = "rust_audit"
version = "0.1.0"
edition = "2021"
description = "A CLI tool to audit Rust projects."
license = "MIT"
repository = "https://github.com/pbizopoulos/canonicalization"
readme = "../../README"
keywords = ["audit", "rust"]
categories = ["development-tools"]
