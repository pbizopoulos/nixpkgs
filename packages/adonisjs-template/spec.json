{
  "testCommand": "nix fmt && nix build .#checks.x86_64-linux.adonisjs-template",
  "selfEditingRules": [
    "Keep this file intent-focused and describe user-visible behavior and constraints rather than implementation details.",
    "Update features and scenarios whenever the implemented behavior changes and the new behavior is intended.",
    "Add, merge, split, or remove features and scenarios to keep the spec aligned with the current intended product behavior.",
    "Prefer adjusting existing scenarios over adding redundant ones when intent can be expressed more clearly by rewriting.",
    "If code and tests conflict with this file, first determine whether the implementation is intentionally different; if so, update this file to match the intended behavior.",
    "Do not change testCommand unless explicitly instructed."
  ],
  "features": [
    {
      "feature": "Landing experience",
      "scenarios": [
        {
          "scenario": "Visitor opens the starter landing page",
          "given": ["the application is running"],
          "when": ["the visitor requests the home page"],
          "then": [
            "the application presents itself as a real AdonisJS web starter instead of a placeholder scaffold",
            "the page highlights sessions, auth, Shield, rate limiting, mail, and Vite-backed assets",
            "the page offers clear next actions for registration, login, dashboard access, docs, and health checks"
          ]
        }
      ]
    },
    {
      "feature": "Registration and sessions",
      "scenarios": [
        {
          "scenario": "Visitor registers a new account",
          "given": ["the application is running with its database available"],
          "when": [
            "the visitor submits a valid username, email address, and password through the registration form"
          ],
          "then": [
            "the application creates a persisted user with hashed credentials",
            "the application signs the visitor into the session immediately",
            "the application sends a welcome mail through the configured mailer"
          ]
        },
        {
          "scenario": "Visitor signs in with valid credentials",
          "given": ["an account already exists"],
          "when": [
            "the visitor submits valid credentials through the login form"
          ],
          "then": [
            "the application authenticates the visitor with session-backed auth",
            "the application redirects the visitor to the protected dashboard",
            "the application exposes a logout action that ends the session"
          ]
        }
      ]
    },
    {
      "feature": "Protected dashboard",
      "scenarios": [
        {
          "scenario": "Authenticated visitor opens the dashboard",
          "given": ["the visitor is signed in"],
          "when": ["the visitor requests the dashboard route"],
          "then": [
            "the application renders account details for the authenticated user",
            "the dashboard offers account-level actions such as logout and deletion",
            "the application keeps the route unavailable to guests"
          ]
        }
      ]
    },
    {
      "feature": "Form protection and operational health",
      "scenarios": [
        {
          "scenario": "Visitor submits authentication forms",
          "given": ["the visitor uses the login or registration forms"],
          "when": ["the visitor sends a state-changing request"],
          "then": [
            "the application requires CSRF protection",
            "the application rate-limits login and signup attempts",
            "the application can return a throttled response after repeated abuse"
          ]
        },
        {
          "scenario": "Operator checks service health",
          "given": ["the application is running with its required services"],
          "when": ["an operator requests the health endpoint"],
          "then": [
            "the application reports overall health status",
            "the response includes database status information",
            "the response includes memory health information"
          ]
        }
      ]
    }
  ]
}
