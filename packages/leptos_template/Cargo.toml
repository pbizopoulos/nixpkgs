[dependencies]
leptos = {version = "0.6", features = ["ssr"]}
tokio = {version = "1.0", features = ["full"]}
axum = "0.7"
leptos_axum = "0.6"

[package]
name = "leptos-template"
version = "0.1.0"
edition = "2021"
