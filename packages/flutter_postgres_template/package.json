{
  "name": "flutter_postgres_template",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "flutter run",
    "format": "flutter format .",
    "build": "flutter build web",
    "start": "echo 'Run flutter run'",
    "test": "npm run test:unit && npm run test:cpd",
    "test:unit": "flutter test",
    "test:cpd": "jscpd .",
    "db:start": "postgres start",
    "db:stop": "postgres stop"
  },
  "devDependencies": {
    "jscpd": "^4.0.5"
  }
}
