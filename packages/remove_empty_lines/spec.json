{
  "testCommand": "nix build .#checks.x86_64-linux.remove_empty_lines",
  "selfEditingRules": [
    "Keep this file intent-focused and describe user-visible behavior and constraints rather than implementation details.",
    "Update features and scenarios whenever the implemented behavior changes and the new behavior is intended.",
    "Add, merge, split, or remove features and scenarios to keep the spec aligned with the current intended product behavior.",
    "Prefer adjusting existing scenarios over adding redundant ones when intent can be expressed more clearly by rewriting.",
    "If code and tests conflict with this file, first determine whether the implementation is intentionally different; if so, update this file to match the intended behavior.",
    "Do not change testCommand unless explicitly instructed."
  ],
  "features": [
    {
      "feature": "Whitespace cleanup",
      "scenarios": [
        {
          "scenario": "User removes blank lines from text files",
          "given": [
            "the user points the command at the current directory or specific paths"
          ],
          "when": ["the command processes text files"],
          "then": [
            "the command removes blank and whitespace-only lines",
            "the command preserves the order of the remaining non-empty lines",
            "the command rewrites files only when their contents change"
          ]
        },
        {
          "scenario": "User runs the command on mixed file types",
          "given": ["the target tree contains binary files and ignored paths"],
          "when": ["the command walks the tree"],
          "then": [
            "the command skips binary files",
            "the command leaves ignored paths alone",
            "the command continues processing other files even if one path errors"
          ]
        }
      ]
    }
  ]
}
