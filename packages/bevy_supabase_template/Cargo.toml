[package]
name = "bevy-supabase"
version = "0.0.0"
edition = "2021"

[dependencies]
bevy = "0.14.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
reqwest = { version = "0.12", features = ["json"] }
tokio = { version = "1", features = ["full"] }

[dev-dependencies]
# No specific dev-deps needed for base bevy test
