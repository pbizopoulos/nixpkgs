{
  "testCommand": "nix fmt && nix build .#checks.x86_64-linux.check_repository_directory_structure",
  "selfEditingRules": [
    "Keep this file intent-focused and describe user-visible behavior and constraints rather than implementation details.",
    "Update features and scenarios whenever the implemented behavior changes and the new behavior is intended.",
    "Add, merge, split, or remove features and scenarios to keep the spec aligned with the current intended product behavior.",
    "Prefer adjusting existing scenarios over adding redundant ones when intent can be expressed more clearly by rewriting.",
    "If code and tests conflict with this file, first determine whether the implementation is intentionally different; if so, update this file to match the intended behavior.",
    "Do not change testCommand unless explicitly instructed."
  ],
  "features": [
    {
      "feature": "Repository layout enforcement",
      "scenarios": [
        {
          "scenario": "Maintainer checks a repository with structural issues",
          "given": ["the command is run inside a git working tree"],
          "when": [
            "the repository contains untracked paths or disallowed file layouts"
          ],
          "then": [
            "the command reports each structural warning in plain text",
            "the command exits with failure",
            "the warnings identify the offending repository paths"
          ]
        },
        {
          "scenario": "Maintainer checks a conforming repository",
          "given": [
            "the repository follows the expected package and host layout"
          ],
          "when": ["the command validates the repository structure"],
          "then": [
            "the command exits successfully",
            "the command accepts the supported template-specific file families, including package specs",
            "the command does not require network access to perform the validation"
          ]
        }
      ]
    }
  ]
}
