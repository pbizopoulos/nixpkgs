[build-dependencies]
tauri-build = {version = "2", features = []}

[dependencies]
tauri = {version = "2", features = []}
serde = {version = "1.0", features = ["derive"]}
serde_json = "1.0"

[features]
custom-protocol = ["tauri/custom-protocol"]

[package]
name = "app"
version = "0.0.0"
description = "Tauri Postgres App"
authors = ["pbizopoulos"]
edition = "2021"
