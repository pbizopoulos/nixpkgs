{
  "testCommand": "nix fmt && nix build .#checks.x86_64-linux.python_latex_template",
  "selfEditingRules": [
    "Keep this file intent-focused and describe user-visible behavior and constraints rather than implementation details.",
    "Update features and scenarios whenever the implemented behavior changes and the new behavior is intended.",
    "Add, merge, split, or remove features and scenarios to keep the spec aligned with the current intended product behavior.",
    "Prefer adjusting existing scenarios over adding redundant ones when intent can be expressed more clearly by rewriting.",
    "If code and tests conflict with this file, first determine whether the implementation is intentionally different; if so, update this file to match the intended behavior.",
    "Do not change testCommand unless explicitly instructed."
  ],
  "features": [
    {
      "feature": "Generated manuscript workspace",
      "scenarios": [
        {
          "scenario": "User renders the template",
          "given": ["the Python and LaTeX toolchains are available"],
          "when": ["the user runs the template program"],
          "then": [
            "the program creates a tmp workspace under the selected output root",
            "the workspace contains a generated figure and a generated LaTeX table",
            "the program compiles the manuscript into a PDF"
          ]
        },
        {
          "scenario": "Developer runs the end-to-end test flow",
          "given": ["the template program is executed with DEBUG=1"],
          "when": ["the test flow completes"],
          "then": [
            "the program verifies that the generated figure, table, and PDF exist",
            "the generated table contains LaTeX tabular markup",
            "the program reports a passing test result"
          ]
        }
      ]
    }
  ]
}
