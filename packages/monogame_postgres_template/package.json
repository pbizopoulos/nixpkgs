{
  "name": "monogame_postgres_template",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "dotnet run",
    "format": "nix fmt",
    "build": "dotnet build",
    "start": "dotnet run",
    "test": "npm run test:unit && npm run test:cpd",
    "test:unit": "dotnet test",
    "test:cpd": "jscpd .",
    "db:start": "postgres start",
    "db:stop": "postgres stop"
  },
  "devDependencies": {
    "jscpd": "^4.0.5"
  }
}
