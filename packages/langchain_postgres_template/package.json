{
  "name": "langchain_postgres_template",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "python app/main.py",
    "format": "nix fmt",
    "build": "echo 'No build step for basic LangChain'",
    "start": "python app/main.py",
    "test": "npm run test:unit && npm run test:cpd",
    "test:unit": "pytest tests/unit",
    "test:cpd": "jscpd .",
    "db:start": "postgres start",
    "db:stop": "postgres stop"
  },
  "devDependencies": {
    "jscpd": "^4.0.5"
  }
}
