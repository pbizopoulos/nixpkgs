{
  "testCommand": "nix fmt && nix build .#deploy_host_template",
  "selfEditingRules": [
    "Keep this file intent-focused and describe user-visible behavior and constraints rather than implementation details.",
    "Update features and scenarios whenever the implemented behavior changes and the new behavior is intended.",
    "Add, merge, split, or remove features and scenarios to keep the spec aligned with the current intended product behavior.",
    "Prefer adjusting existing scenarios over adding redundant ones when intent can be expressed more clearly by rewriting.",
    "If code and tests conflict with this file, first determine whether the implementation is intentionally different; if so, update this file to match the intended behavior.",
    "Do not change testCommand unless explicitly instructed."
  ],
  "features": [
    {
      "feature": "Host provisioning workflow",
      "scenarios": [
        {
          "scenario": "Operator deploys the host template",
          "given": ["the required secrets and cloud credentials are available"],
          "when": ["the operator runs the deployment command"],
          "then": [
            "the workflow initializes its infrastructure state",
            "the workflow provisions the target cloud server and runs the NixOS deployment step",
            "the workflow persists deployment state under the package tmp directory"
          ]
        },
        {
          "scenario": "Operator needs the deployed server address",
          "given": ["the deployment succeeds"],
          "when": ["the workflow finishes applying infrastructure changes"],
          "then": [
            "the workflow writes the provisioned IPv4 address into the package output directory",
            "the recorded address can be used by follow-up operational steps"
          ]
        }
      ]
    }
  ]
}
