{
  "testCommand": "nix fmt && nix build .#checks.x86_64-linux.fastapi_postgres_template",
  "selfEditingRules": [
    "Keep this file intent-focused and describe user-visible behavior and constraints rather than implementation details.",
    "Update features and scenarios whenever the implemented behavior changes and the new behavior is intended.",
    "Add, merge, split, or remove features and scenarios to keep the spec aligned with the current intended product behavior.",
    "Prefer adjusting existing scenarios over adding redundant ones when intent can be expressed more clearly by rewriting.",
    "If code and tests conflict with this file, first determine whether the implementation is intentionally different; if so, update this file to match the intended behavior.",
    "Do not change testCommand unless explicitly instructed."
  ],
  "features": [
    {
      "feature": "Landing experience",
      "scenarios": [
        {
          "scenario": "Visitor opens the starter landing page",
          "given": ["the application is running"],
          "when": ["the visitor requests the home page"],
          "then": [
            "the application presents itself as a real FastAPI starter rather than a placeholder scaffold",
            "the page highlights server-rendered templates, sessions, CSRF, PostgreSQL-ready persistence, and health reporting",
            "the page offers clear next actions for registration, login, and dashboard access"
          ]
        }
      ]
    },
    {
      "feature": "Registration and session auth",
      "scenarios": [
        {
          "scenario": "Visitor registers a new account",
          "given": [
            "the application is running with its configured database available"
          ],
          "when": [
            "the visitor submits a valid username, email address, and password through the registration form"
          ],
          "then": [
            "the application creates a persisted user record",
            "the application signs the visitor into the session immediately",
            "the application makes the authenticated dashboard available"
          ]
        },
        {
          "scenario": "Visitor signs in and manages the account",
          "given": ["an account already exists"],
          "when": [
            "the visitor submits valid credentials through the login form"
          ],
          "then": [
            "the application authenticates the visitor with session-backed auth",
            "the application provides logout and account deletion actions for the active session",
            "guest users cannot access the protected dashboard content"
          ]
        }
      ]
    },
    {
      "feature": "Form protection and health reporting",
      "scenarios": [
        {
          "scenario": "Visitor submits authentication forms",
          "given": ["the visitor uses the login or registration flows"],
          "when": ["the visitor sends state-changing requests"],
          "then": [
            "the application requires a valid CSRF token",
            "the application can throttle repeated login and signup attempts",
            "the application returns a clear throttling response when limits are exceeded"
          ]
        },
        {
          "scenario": "Operator checks application health",
          "given": ["the application is running"],
          "when": ["an operator requests the health endpoint"],
          "then": [
            "the response reports overall health status",
            "the response includes database health information",
            "the response includes memory health information"
          ]
        }
      ]
    }
  ]
}
